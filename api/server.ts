// This file exists so Vercel's CLI will detect the server entrypoint and not throw the following error.
// Error: The pattern "src/server.ts" defined in `functions` doesn't match any Serverless Functions inside the `api` directory.

export * from "../src/server";
